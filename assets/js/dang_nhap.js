const userName = $("#username");
const password = $("#password");
const loginBtn = $("login__link-to-Homepage");

loginBtn.onclick = () => {
    if(userName.value.trim() == '' || password.value.trim() == '') 
}
